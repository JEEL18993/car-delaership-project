{
  "name": "car-dealership-inventory-system",
  "version": "1.0.0",
  "description": "Root workspace runner for Car Dealership Inventory System",
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run dev",
    "seed": "npm run seed:admin --prefix backend",
    "test": "npm test --prefix backend && npm test --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
